<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../includes/head.ejs') %>
    <style>
      body {
        background: linear-gradient(135deg, #f0fdfa 0%, #dbeafe 50%, #fef2f2 100%);
        background-attachment: fixed;
        min-height: 100vh;
        display: flex;
        align-items: center;
        position: relative;
        overflow: hidden;
      }
      body::before {
        content: "";
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(circle, rgba(13, 148, 136, 0.05) 0%, transparent 70%);
        animation: rotate 20s linear infinite;
      }
      @keyframes rotate {
        from {
          transform: rotate(0deg);
        }
        to {
          transform: rotate(360deg);
        }
      }
      .container {
        position: relative;
        z-index: 10;
      }
      .login-card {
        margin-top: 100px;
        border: none;
        border-radius: 1.5rem;
        box-shadow: 0 20px 50px rgba(0, 0, 0, 0.15);
        overflow: hidden;
        position: relative;
        background: white;
      }
      .login-card::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 6px;
        background: linear-gradient(90deg, #0d9488, #0284c7, #f97316, #ef4444);
      }
      .login-card .card-body {
        padding: 3rem !important;
      }
      .login-card .card-title {
        background: linear-gradient(135deg, #0d9488, #0284c7);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        font-weight: 800;
        font-size: 2rem;
        letter-spacing: -0.5px;
      }
      .login-card img {
        width: 70px !important;
        filter: drop-shadow(0 4px 12px rgba(13, 148, 136, 0.3));
      }
      .form-control {
        border: 2px solid #e2e8f0;
        border-radius: 0.75rem;
        padding: 0.9rem 1.2rem;
        font-size: 1rem;
        transition: all 0.3s ease;
      }
      .form-control:focus {
        border-color: #0d9488;
        box-shadow: 0 0 0 3px rgba(13, 148, 136, 0.1);
      }
      .form-group label {
        font-weight: 600;
        color: #334155;
        margin-bottom: 0.5rem;
        font-size: 0.95rem;
      }
      .btn-login {
        background: linear-gradient(135deg, #0d9488, #0284c7, #3b82f6);
        border: none;
        color: white;
        padding: 1rem;
        font-weight: 700;
        border-radius: 0.75rem;
        font-size: 1.05rem;
        letter-spacing: 0.5px;
        transition: all 0.3s ease;
        box-shadow: 0 8px 20px rgba(13, 148, 136, 0.3);
      }
      .btn-login:hover {
        transform: translateY(-3px);
        box-shadow: 0 12px 28px rgba(13, 148, 136, 0.4);
        background: linear-gradient(135deg, #0284c7, #3b82f6, #0d9488);
      }
      a {
        color: #0d9488;
        font-weight: 600;
        transition: all 0.2s ease;
      }
      a:hover {
        color: #f97316;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
          <div class="card login-card">
            <div class="card-body p-5">
              <div class="text-center mb-4">
                <img src="/images/tbcare_landing/logo.png" alt="Logo" style="width: 50px" />
                <h3 class="card-title mt-2">TBCare Login</h3>
              </div>

              <form action="/login" method="POST">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                <div class="form-group">
                  <label for="email">Email address</label>
                  <input type="email" class="form-control" id="email" name="email" required />
                </div>
                <div class="form-group">
                  <label for="password">Password</label>
                  <input type="password" class="form-control" id="password" name="password" required />
                </div>
                <button type="submit" class="btn btn-login btn-block mt-4">Sign In</button>
                <div class="text-center mt-3">
                  <a href="/reset">Forgot Password?</a>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
    <%- include('../includes/end.ejs') %>
  </body>
</html>